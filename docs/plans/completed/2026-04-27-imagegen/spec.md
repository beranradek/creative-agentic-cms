In addition to already existing image upload functionality, 
Add "Generate image" functionality to editor, that will open dialog with form - text area 
for description of desired image to be generated, select with output size, quality - and 
add new LangChain.js agent for generating images as new assets. 
Inspire yourself by already existing example from another project: docs/examples/imagegen. 
This should be implemented as part of mission/scope as new feature.